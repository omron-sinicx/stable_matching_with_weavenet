# --------- pytorch --------- #
torch>=1.10.0
torchvision>=0.11.0
pytorch-lightning>=2.0
torchmetrics>=1.0

# --------- hydra --------- #
hydra-core>=1.3
hydra-colorlog>=1.2

# --------- loggers --------- #
# wandb
# neptune-client
# mlflow
# comet-ml

# --------- others --------- #
pyrootutils     # standardizing the project root setup
torchviz        # computational graph visualization (used in weavenet_module.py)
rich            # beautiful text formatting in terminal
pre-commit      # hooks for applying linters on commit
pytest          # tests
# sh            # for running bash commands in some tests (linux/macos only)
# weavenet is vendored at external/weavenet/ (a snapshot of upstream v1.1.0)
# and installed editable by .devcontainer/post-create.sh. The vendoring is
# transitional; once you would rather pull from PyPI, see
# external/weavenet/NOTES.md for the removal procedure and use
# `weavenet>=1.1.0` here instead.
