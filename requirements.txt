# --------- pytorch --------- #
torch>=1.10.0
torchvision>=0.11.0
pytorch-lightning>=2.0
torchmetrics>=1.0

# --------- hydra --------- #
hydra-core>=1.3
hydra-colorlog>=1.2

# --------- loggers --------- #
# wandb
# neptune-client
# mlflow
# comet-ml

# --------- others --------- #
pyrootutils     # standardizing the project root setup
torchviz        # computational graph visualization (used in weavenet_module.py)
rich            # beautiful text formatting in terminal
pre-commit      # hooks for applying linters on commit
pytest          # tests
# sh            # for running bash commands in some tests (linux/macos only)
# weavenet is vendored at external/weavenet/ and installed editable by
# .devcontainer/post-create.sh. See external/weavenet/NOTES.md for the
# upstream PRs this snapshot carries and the plan to switch back to PyPI
# once weavenet v1.0.2 is tagged.
